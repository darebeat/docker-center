version: '3'
services:
  mysql:
    image: mysql:5.7
    container_name: mysql
    restart: always
    networks:
      deploy:
        ipv4_address: 172.10.0.101
        aliases:
          - mysql
    ports:
      - "3310:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=mysql
      - MYSQL_USER=test
      - MYSQL_PASSWORD=test0755
      - MYSQL_DATABASE=test
      - TZ=Asia/Shanghai
    volumes:
      - ./db:/var/lib/mysql:rw
      - ./src/config/mysqld.cnf:/etc/mysql/mysql.conf.d/mysqld.cnf
    tty: true

networks:
  deploy:
    external: true